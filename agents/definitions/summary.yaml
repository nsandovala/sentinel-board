name: summary
label: Summary Employee
purpose: Resumir cambios recientes, foco del día y avances por proyecto.
input:
  - projects
  - cards
  - events
output:
  - daily_summary
  - project_updates
mode: advisory